

function Point(x, y) {
    this.x = x
    this.y = y
}

function Area(x, y, w, h) {
    this.x = x
    this.y = y
    this.w = w
    this.h = h
}

function Socket(name,)

/**
 * 
 * @param {String} name 
 */
function Symbol(name) {
    this.name = name
    this.area = new Area()
    this.
}